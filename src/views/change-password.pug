extends base

block content 
    if errorMessage 
        span=errorMessage
    form(action="", method="POST")
        input(name="oldPassword", type="text", placeholder="Old Password", required)
        input(name="newPassword", type="text", placeholder="New Password", required)
        input(name="newPasswordConfirm", type="text", placeholder="New Password Confirm", required)
        input(type="submit", value="Save")