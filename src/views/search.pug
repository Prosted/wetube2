extends base
include mixins/video

block content 
    form(method="GET")
        input(name="keyword", type="text", required, placeholder="Enter Your Keword")
        input(type="submit", value="Search")

    each video in videos
        +video(video)

