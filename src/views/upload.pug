extends base

block content
    form(action="", method="POST")
        input(name="title" type="text", placeholder="enter youre new video's title", required)
        input(type="submit", value="Save")